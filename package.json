{
  "name": "zinc-registry",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ Enenra06/zinc-registry",
    "create-local": "graph create --node http://localhost:8020/ Enenra06/zinc-registry",
    "remove-local": "graph remove --node http://localhost:8020/ Enenra06/zinc-registry",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 Enenra06/zinc-registry"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.13.3",
    "@graphprotocol/graph-ts": "0.13.0"
  }
}
